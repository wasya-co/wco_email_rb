
.sites-index.maxwidth
  %h5
    Sites
    = link_to '[+]', new_site_path

  %table.bordered
    %tr
      %th &nbsp;
      %th origin
      %th post_path
    - @sites.each do |s|
      %tr
        %td
          = button_to '[x]', site_path(s), method: :delete, data: { confirm: 'Are you sure?' }
        %td
          = link_to s.origin, edit_site_path(s)
        %td= s.post_path
